name: Continuous Deployment (AWS EC2 Coolify)

on:
  push:
    branches: ["main"]

jobs:
  deploy-backend:
    name: Deploy Backend to AWS EC2 via Coolify Webhook
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Coolify Deployment Webhook
        env:
          COOLIFY_WEBHOOK_URL: ${{ secrets.COOLIFY_WEBHOOK_URL }}
        run: |
          if [ -z "$COOLIFY_WEBHOOK_URL" ]; then
            echo "::error::COOLIFY_WEBHOOK_URL secret is not set in GitHub repository secrets."
            exit 1
          fi
          echo "Triggering Coolify deployment for supersparton/CareerAtlas:main on AWS EC2..."
          
          # Convert https:// to http:// if webhook points to an IP/port without SSL
          HTTP_URL=$(echo "$COOLIFY_WEBHOOK_URL" | sed 's/https:\/\//http:\/\//g')
          
          curl -k -s -X POST "$COOLIFY_WEBHOOK_URL" || \
          curl -k -s -X GET "$COOLIFY_WEBHOOK_URL" || \
          curl -s -X POST "$HTTP_URL" || \
          curl -s -X GET "$HTTP_URL"
          
          echo ""
          echo "Coolify deployment webhook successfully triggered!"
